<Window x:Class="MijnBibliotheekWPF.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Aanmelden"
        Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#F2F2F2">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Mijn Bibliotheek" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0 0 0 10"/>

        <StackPanel Grid.Row="1" Margin="0 10 0 10">
            <TextBlock Text="E-mail" FontWeight="SemiBold"/>
            <TextBox x:Name="EmailBox" Height="28" Margin="0 4 0 10"/>

            <TextBlock Text="Naam (voor registratie)" FontWeight="SemiBold"/>
            <TextBox x:Name="NaamBox" Height="28" Margin="0 4 0 10"/>

            <TextBlock Text="Wachtwoord" FontWeight="SemiBold"/>
            <PasswordBox x:Name="PwBox" Height="28" Margin="0 4 0 10"/>

            <Button Content="Inloggen" Height="32" Click="Login_Click" Margin="0 8 0 2" Background="#4E73DF" Foreground="White"/>
            <Button Content="Registreren" Height="30" Click="Register_Click" Background="#A0A0A0" Foreground="White"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="2" HorizontalAlignment="Center" Foreground="Red" Margin="0 6 0 0"/>
    </Grid>
</Window>
